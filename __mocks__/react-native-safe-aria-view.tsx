//import React from "react";

/*jest.mock(
    'react-native-safe-area-view',
    () =>
        class MockSafeAreaView extends React.Component {
            render() {
                const { children , ...otherProps } = this.props
                return React.createElement('SafeAreaView', otherProps, children)
            }
        },
)*/

/* import {SafeAreaView} from "react-native";
const React = require('react');
type Props = { children: any };
const safeAreaMockRenderFunction = ({ children, ...otherProps }: Props) => {
   // React.createElement('SafeAreaView', otherProps, children);
};
export default safeAreaMockRenderFunction; */
